## Development
  Create `.env` file. Look `example.env`.
  Update `docker-compose.env` file.
  ```sh
  docker-compose up -d postgres
  ```
  ```sh
  npm install
  ```
  ```sh
  npm run start
  ```

## Prod
  Update `docker-compose.env` file.
  ```sh
  docker-compose up -d
  ```

### Swagger
  `http://host:port/docs`